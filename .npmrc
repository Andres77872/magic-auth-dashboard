# pnpm configuration
# Install missing peer dependencies automatically (pnpm default, made explicit).
auto-install-peers=true
# Don't fail the whole install on peer-dependency version mismatches; surface as warnings.
strict-peer-dependencies=false
